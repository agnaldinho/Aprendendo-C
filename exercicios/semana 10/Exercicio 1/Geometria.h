#ifndef _Geometria_h
#define _Geometria_h

// Definir O QUE vai fazer

float PerimetroQuadrado(float lado);

float PerimetroCirculo(float raio);

float VolumeCubo(float lado);

float VolumeEsfera(float raio);

#endif
